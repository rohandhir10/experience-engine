# Dependencies for web/api/engine.py (the Vercel Python function). Vercel's
# Python builder reads this from the project's Root Directory.
#
# Deliberately NOT the full repo-root requirements.txt:
#   - uvicorn: not needed, Vercel serves the ASGI app itself.
#   - anthropic, youtube-transcript-api, deep-translator, pytest: unused by
#     the /api/adapt path.
#   - sudachipy, sudachidict-core: the Japanese kanji-reading dependency.
#     This is the exact package whose download timed out during local
#     install earlier in this project — excluding it here avoids the same
#     failure during Vercel's build. The engine already degrades
#     gracefully without it (returns None rather than an undercount for
#     kanji-bearing text; pure-kana Japanese still counts exactly) — a
#     real, disclosed capability loss for Japanese on this deployment,
#     not a silent one.
openai>=1.50.0
pydantic>=2.6
fastapi>=0.110.0
pronouncing>=0.2.0
