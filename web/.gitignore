node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Copied from node_modules/pdfjs-dist by scripts/copy-pdf-worker.js on
# every `npm install` (see that script's comment) - a build artifact of
# an installed dependency, not source.
public/pdf.worker.min.mjs
