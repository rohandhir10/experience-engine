"""Standalone operational scripts, run directly (e.g. `python -m
scripts.create_test_user`) rather than imported by the FastAPI app
itself - see each script's own docstring for what it does and why.
"""
